/*#pragma once
#include"DX_Lib/DXLib_Orig.h"


class FontTexture {
public:
	static FontTexture* GetInstance();
	FontTexture();
	static void Release();
	void Init();
	int SaveImage(const wchar_t* ch);
	void LoadImage(int Identifier, int Stage);
	void Reset();
private:
	static FontTexture* SingleTon;
	int ImageIdentifier;
	vector<LPDIRECT3DTEXTURE9*> images;
};/*/

